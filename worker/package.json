{
  "name": "williamson-wallflowers-inquiry",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js",
    "test": "node --test tests/*.test.js",
    "bridge": "node scripts/wallflower-bridge.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply williamson-wallflowers-moments --local",
    "migrate:remote": "wrangler d1 migrations apply williamson-wallflowers-moments --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
